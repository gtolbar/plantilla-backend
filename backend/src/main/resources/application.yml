server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost/persona
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
